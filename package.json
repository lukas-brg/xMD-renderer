{
    "name": "xmd",
    "private": true,
    "workspaces": [
        "xmd-parser",
        "xmd-code"
    ],
    "scripts": {
        "build": "npm run build --workspace=xmd-parser && npm run build --workspace=xmd-code",
        "test": "npm test --workspaces",
        "code": "code --extensionDevelopmentPath=${PWD}/xmd-code",
        "start": "npm run build --workspace=xmd-parser && npm run start --workspace=xmd-parser"
    }
}
